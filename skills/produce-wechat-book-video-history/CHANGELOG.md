# Changelog

## 3.11.0 - 2026-08-12

- 在正常二创路径新增 O01 文皮皮原创检测硬门：参考原文放左框、二创稿放右框，网站明确显示“鉴定结果：原创”后才允许进入 G02 文案确认。
- 新增可重复执行的 Playwright 检测脚本，保存原始相似度、有效运行字段、完整网页截图和机器可读报告；禁止把页面初始“0% / 原创”占位状态误当通过。
- 非原创结果必须改稿后复检；网站、容量、登录、浏览器或结果校验不可用时阻塞，禁止静默替换检测服务。成稿直出因不发生二创而记录 O01 不适用。

## 3.10.2 - 2026-08-04

- 豆瓣返回同一本书的多个版本且用户未明确指定版本时，默认采用出版日期最新且可唯一确认的版本。
- 用户指定的豆瓣条目或 ISBN 始终优先；日期缺失、无法解析、精度不足或最新日期并列时仍暂停确认，避免误选。

## 3.10.1 - 2026-08-04

- 按用户提供的参考图替换默认女性角色外貌：柔和椭圆脸、鼻梁与上脸颊自然淡雀斑、深棕杏眼、黑色松散高发髻和脸侧碎发。
- 默认服装替换为宽松炭灰色粗纹理麻花针织圆领毛衣与深炭灰休闲长裤；不改变既有画风、色盘、光线、构图、分镜或生产流程。
- 明确风格锚点中的人物与衣着不作为身份依据，当前锁定角色提示词优先。

## 3.10.0 - 2026-08-03

- 微信读书成功搜索但没有匹配书籍时，允许使用豆瓣读书补齐书名、作者、译者、出版社、出版日期、ISBN、版本候选和封面来源。
- 新增受控的 Python 3 单书查询脚本；一次查询最多读取 5 个候选详情，不运行旧仓库的无限标签爬取，也不绕过验证码、登录或限流；人工选版后复用已保存候选，不重复搜索。
- 豆瓣仅作为书籍元数据与封面回退，不能替代微信读书热门划线、书中原文或二创文案证据；多版本无法唯一确认时必须暂停。

## 3.9.2 - 2026-08-02

- 将 APIMart 正式生产背景图规格锁定为用户确认的 `9:16 + 1k`，本通道实测输出 `941×1672`。
- 最终视频画布继续保持 `1080×1920`，源图在确定性渲染阶段等比适配。

## 3.9.1 - 2026-08-02

- 自动从本机 kit 识别 APIMart Base URL，并适配其异步任务提交、轮询和图片下载协议。
- APIMart 竖屏生图固定使用 `9:16` 与 `2k`，G04 和单图重做通过 `image_urls` 传入参考图。
- 异步任务提交后若轮询中断，不自动重新提交，避免重复生图和重复计费。

## 3.9.0 - 2026-08-02

- 将 G03 样图、G04 剩余分镜、单图修改和生成式封面底图统一切换到本机 `GPTImage2.txt` API kit，并固定使用 `gpt-image-2`。
- 禁止调用 Codex 内置 `imagegen` 或静默回退其他生图服务；Key 仅在运行时读取，不进入项目产物、日志或仓库。
- G04 使用已通过的 G03 样图作为编辑接口风格输入，单图重做使用当前图片作为编辑输入，同时保留原有 9:16、暖金电影感、安全区、无文字和质量检查规格。

## 3.8.5 - 2026-08-02

- 将视频号正式发布前的“原创”和“含有AI生成内容”升级为失败即停止的强制验证项。
- 禁止发布器回退选择“无需声明/不声明/无”；声明无法找到、选择或复核时不得点击发表。
- 保存发布前页面截图作为声明证据，并修复本机 Patchright 异步入口必须在独立 Proactor 线程运行的兼容性问题。

## 3.8.4 - 2026-08-02

- 长标题 `L01–L10` 调整为每个包含 20–40 个中文汉字，来源标题只保留口语节奏、情绪强度和标点参考，不再限制候选长度。
- 每个长标题自然组合 2–3 个不同的 `dbs-xhs-title` 公式角度，并覆盖 3–6 个来自书名/主题、文案、受众问题、情绪、行动或结果的非重复关键词。
- 10 个候选必须改变公式组合、关键词集合、开头结构和冲突/收益侧重；禁止关键词堆砌、近义词叠放、重复分句和未经证实的承诺，并在 `titles.json` 中保存组合公式、关键词与变化维度。

## 3.8.3 - 2026-08-02

- 短标题 `S01–S10` 强制至少包含 8 个中文汉字；标点、英文字母、数字和空格不计入最低数量。
- 推荐范围由 4–12 个中文字符调整为 8–12 个，保留完整短标题最多 16 个字符的上限；不足 8 个中文汉字的候选必须重生成，不能进入自动采用或用户展示。

## 3.8.2 - 2026-08-02

- 将用户在《山茶的情书》V2 中确认的柔和女声母带保存为默认 `female-book-narrator-locked-v1` 处理流程；保留原参考音频、VoxCPM2 参数、固定 seed、语速和时间轴。
- 女声默认处理改为 65Hz 高通、12kHz 低通、250Hz +1dB、3.2kHz -2dB、1.2:1 轻压缩、-15.5 LUFS 目标和 -1.5dBTP 峰值，减少偏硬的存在感并保留温暖度。
- `finalize_mix.py`、默认配置、女声预设和技术规范统一使用同一套参数；最终混音不再对女声额外衰减为 0.65 倍。

## 3.8.1 - 2026-08-01

- 禁止在成片任意时间点或独立封面中烧录 AI 生成声明，明确拦截“本视频含AI生成画面与AI合成配音”及同类提示。
- 平台要求的 AI 内容声明改为仅在发布设置、声明字段或发布文案中处理，不进入片头、正文、字幕、片尾或封面。
- C02 新增成片与封面不存在烧录式 AI 声明的检查项。

## 3.8.0 - 2026-08-01

- 新增经单次任务明确授权后的多平台正式发布流程，首批支持抖音与视频号。
- 保留视频号草稿箱兼容入口；正式发布必须选择账号并提交 `AUTO_PUBLISH_CONFIRMED` 授权，不能由文案、图片或交付确认推断。
- 每个平台、账号和成片版本使用独立幂等键，支持部分失败后只重试失败目标，并保存逐平台发布结果。
- 视频号使用独立 1080x1260 封面；抖音不强行复用不兼容的视频号封面规格。

## 3.7.0 - 2026-08-01

- 将用户确认的暖金生活电影感拼图保存为 `assets/style-references/warm-cinematic-literary-life-v1.png`，并升级为所有 G03/G04 生图默认引用的锁定风格锚点。
- 默认画风改为 `warm-cinematic-literary-life-v1`：低饱和暖中性色、晨昏低角度自然金光、长柔影、轻雾、真实材质、细微胶片颗粒和克制的文学情绪；取消“每张必须一个橙色点缀”。
- 角色镜头默认保持成年东亚女性、肩长微卷黑发、燕麦色长大衣与蓝灰围巾的一致性；分镜继续混合人物、空建筑、静物、道路、公园、山景和城市远景。
- 风格锚点仅用于画风条件控制，禁止复制拼图布局、具体场景、人物姿势和物件位置；参考图中的镜面场景不解除默认倒影禁用规则。
- G03/G04 质检新增风格锚点、人物与服装连续性、暖金光线、低饱和色盘、胶片颗粒/薄雾和镜头语法多样性核验。

## 3.6.0 - 2026-07-31

- 将默认背景图更新为 `bright-natural-film-orange-accent-v1`：明亮清晨自然光、米白浅灰环境、真实生活电影感、轻微胶片颗粒和单一低饱和橙色点缀。
- 默认人物禁止油亮精修、瓷感皮肤、商业棚拍和时尚大片质感；增加欠曝、黑场压抑、过曝、HDR、宽幅橙色染色与青橙调色阻断规则。
- 分镜必须记录 `faceOrientation`、`cameraDistance` 和 `narrativeFunction`，并执行序列级重复审计；禁止连续三个角色镜头使用相同朝向，也禁止整套被侧脸主导。
- 在文案语义支持时混合正脸、三分之二侧脸、背影、手部/物件特写、空镜、建筑、天气与风景，但不以固定配额或无关风景凑数。

## 3.5.0 - 2026-07-31

- 将默认背景图锁定为 `monochrome-ember-editorial-v1`：近黑纹理背景、黑白纪实摄影与简洁插画拼贴、单一低饱和暖橙点色、低调舞台式侧光和克制的文学情绪。
- 新增可执行的风格 profile、统一正向提示词、负面提示词、暖橙占比上限和 G03/G04 风格一致性质检；除非用户明确要求新的视觉方向，后续项目不得静默回退到旧的彩色油画风。
- 参考视频抽帧只作为风格证据，不进入生成素材，不复刻其中的人物、具体构图、书名排版、字幕或水印；所有正式背景图继续要求原创、无文字、无标志、无水印。
- 允许大面积近黑负空间，但必须保留细微纸张/胶片颗粒、明暗渐变和可感知空间深度，禁止用纯黑平涂伪造留白。

## 3.4.0 - 2026-07-31

- 新增显式“成稿直出”分支：用户同时提供可用完整口播稿并明确说“成稿直出”时，原稿逐字写入 `script.txt`，跳过二创生成与常规 G01/G02 停顿。
- “成稿直出”指令本身计为第一次文案确认；C01 通过后自动进入 10+10+10 标题与话题、锁定配音、真实时间轴、语义分镜、样图质检和全部图片生成，仍在 G04 停止等待图片确认。
- 直出模式不把用户文案伪装为已核验书摘，也不推断图片批准、草稿上传或正式发布授权；缺失版本、作者或书封证据只在后续相关阶段暴露。
- 固定片头与正文开场语义重复时，保持 `script.txt` 不变，只允许正文配音省略重复引导语并从 `《书名》` 开始，同时写入配音韵律审计记录。

## 3.3.1 - 2026-07-30

- 将 1080×1920 双语字幕位置锁定为此前确认的标准基线：中文 `MarginV=560`、英文 `MarginV=510`，并强制中英文使用独立 ASS 样式和相同时间边界。
- 禁止把双语字幕合并为单个 `\N` 双行样式，避免字幕回落到画面底部。
- 成片渲染前必须核验舒缓感性旁白参数；遗留的 1.00×、书名后 1.8 秒和普通停顿约 0.55 秒配置将阻断渲染。

## 3.3.0 - 2026-07-30

- 将发布话题从单组固定结果升级为与长标题、短标题并列的自动选择项：生成 `T01–T10` 共 10 组话题候选并默认采用 `T01`。
- 每组严格包含 7 个不重复、空格分隔的井号话题，必含 `#读书`、`#好书推荐` 和按核验书名解析的 `#《书名》`，其余 4 个按文案与采用长标题匹配。
- `titles.json` 必须保存全部话题候选、推荐理由和采用结果；当前对话必须完整展示 10+10+10，并支持回复 `长标题 L04，短标题 S07，话题 T03` 重选。
- 更换长标题时同时废弃并重生成 10 个短标题和 10 组话题候选。

## 3.2.0 - 2026-07-29

- 新增用户直接粘贴完整口播文案的文字入口；原文立即保存为 `raw-transcript-user.txt`，不再下载视频或运行 Whisper。
- 保留原有链接入口：只有链接且没有可用口播正文时，继续执行 TikHub 下载、Whisper 转写和最小校对。
- 同一输入同时包含完整文案和来源链接时优先采用文字入口，链接仅作为来源元数据；明确要求视频复核时才下载。
- 两条入口在 G01 汇合，统一识别书名、核验微信读书版本，并保存热度顺序前 10 条全书热门划线。

## 3.1.1 - 2026-07-28

- 将 1080×1920 成片的中文字幕默认渲染字号从 58 调整为 68。

## 3.1.0 - 2026-07-28

- 自动采用标题后，必须在当前对话同时链接或附上 `titles.json`，并完整展示 10 个长标题和 10 个短标题，不再只展示采用项。
- 长短标题分别使用 `L01–L10`、`S01–S10` 编号并标记当前采用项；用户可按编号立即重选。
- 更换长标题时自动废弃旧短标题、重新生成 10 个短标题，并再次发送完整替换文件与候选列表。

## 3.0.0 - 2026-07-27

- 从默认生产、技术验收、交付清单和飞书同步中移除剪映草稿。
- G04 确认后只生成成片、独立封面、标题文档和验证报告；微信视频号后台草稿上传仍保持为单独的显式授权动作。
- 项目初始化不再创建 `jianying_draft/`，历史草稿文件保持不变且不会被删除。

## 2.0.7 - 2026-07-27

- 书名识别后，工作台项目主标题与复用的 Codex 任务标题统一改为当前书名。
- 用户纠正书名时同步更新任务标题和日期书名目录；抖音原标题继续作为来源证据保留。

## 2.0.6 - 2026-07-27

- 将 `SKILL.md` 收敛为单一总控入口，通过一层 `references/` 按阶段加载详细规则，移除入口中的大段重复说明。
- 修复后期步骤重复、旧标题确认提示、G06 联合审核术语和标题区高度冲突。
- 为超过 100 行的引用文档增加目录，并同步两次确认与自动标题选择口径。
- 清理 Skill 目录中的 Python 缓存文件，不改变两次确认、生产产物或发布边界。

## 2.0.5 - 2026-07-27

- 新增固定发布话题：`#读书 #好书推荐 #人生感悟 #认知成长 #自我提升 #文字的力量 #《书名》`。
- `#《书名》` 必须按已核验的当前书名动态替换，禁止在交付物中保留占位符。
- 发布话题写入 `titles.json` 和 `delivery-manifest.json`，并在视频最终交付消息中一并展示。

## 2.0.4 - 2026-07-27

- 将包含 10 个长标题、10 个短标题、公式溯源和采用结果的 `titles.json` 升级为正式交付物。
- 视频完成后的最终交付必须再次显示已采用的长短标题，并附上 `titles.json` 的可点击链接。
- `delivery-manifest.json` 或用户可见交付消息缺少标题候选文档时，不得宣称交付完成。

## 2.0.3 - 2026-07-27

- 长短标题仍自动生成并采用首个推荐项，但选定后必须立即在当前对话发送非阻塞反馈卡。
- 反馈卡默认展示已采用的长标题、短标题、`titles.json` 路径、10+10 候选数量和回退提示；不等待回复即可继续生产。
- 禁止只把标题结果留在文件、飞书、日志或最终交付消息中；重选后必须补发新的反馈卡并标记旧组合已替代。

## 2.0.2 - 2026-07-27

- 生图默认避开镜子以及承载人物倒影的玻璃、水面、抛光金属和屏幕；自我观察优先改用呼吸、姿态、手记、空间、光线或有意义的物件表达。
- 只有用户明确要求时才允许人物镜像；此时强制核对身份、姿势、视线、肢体数量、左右手、物件位置、透视和光线。
- 镜像不一致升级为发布阻塞项，必须重做或修正，不得用裁切、字幕、模糊、暗化或运镜掩盖。

## 2.0.1 - 2026-07-27

- 将“人物不是默认主体”升级为可检查的生图流程：每个分镜记录 `subjectMode`、`characterNecessity`，必要人物镜头必须说明 `characterJustification`。
- G03 前新增主体混合审计；全人物分镜必须二次规划空间、风景、静物、建筑或天气镜头，除非每个人物镜头均有逐镜叙事理由。
- 不设置固定人物/非人物比例，不为凑多样性加入无关风景，也不为维持连续性强塞装饰性人物。

## 2.0.0 - 2026-07-26

- 将默认生产流程收敛为两次人工确认：第一次确认二创文案，第二次确认全部分镜图片。
- G01 来源包改为自动核验并锁定；长短标题自动生成并采用首个推荐项；通过自动质检的风格样图直接续跑余下图片。
- 第二次确认后自动完成后期、C02 与交付登记，不再增加成片联合确认；正式发布仍需用户另行明确授权。
- 保留所有候选、产物、审计记录和显式回退能力，异常阻塞不算常规确认门。

## 1.1.3 - 2026-07-24

- 将“读书分享、书名、作者/译者”固定文字作为一个整体，相对旧版顶部布局下移约画面高度 15%。
- 分镜构图在画面约 18%–30% 高度预留低干扰文字带，避免固定文字紧贴顶部，同时不制造纯色空块。
- 成片交付前增加固定文字组位置抽帧检查。

## 1.1.2 - 2026-07-23

- 删除“60 秒固定 7–8 张”及“一分钟 8–12 个视觉段落”等固定总数规则。
- 分镜图片数量改为由文案的观点、动作、场景、情绪和叙事功能变化自然决定。
- 保留“约 8 秒一张”作为语义切分后的节奏软参考，允许根据信息密度和完整叙事单元灵活缩短或延长。

## 1.1.0 - 2026-07-23

- 新增长标题、短标题两级阻塞选择：先用抖音原标题与 `dbs-xhs-title` 生成 10 个可追溯长标题，确认后再生成 10 个短标题。
- 每个长标题保存公式编号、触发类型、模板、原始爆款例子和推荐理由；重新生成或更换长标题会清空短标题状态。
- 工作台界面与服务端场景图执行器同时校验标题完成状态，未确认长短标题时禁止继续生图。

## 1.0.0 - 2026-07-23

- 建立 TikHub 下载、Codex/Whisper 转写、DBS 文案诊断和微信读书热门划线取证流程。
- 固化 G01–G08 阻塞确认门及飞书状态同步。
- 默认使用 `female-book-narrator-locked-v1` 女声与女版固定片头；男声仅在用户明确要求时启用。
- 支持分镜图片、真实配音时长、双语字幕、1080x1920 60fps 成片、可编辑剪映草稿和独立视频号封面。
- 加入版本文件；后续改动按语义化版本递增并同步到 `a798221115-byte/Book-Video-Factory`。
